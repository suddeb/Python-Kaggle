def can_drive(age):
    """can_drive?
    # As per the rule, you need to be atleast 18 years old to drive car
    Args:
        age (int): age of the person
    """
    return age >=18

print(can_drive(12))
print(can_drive(23))